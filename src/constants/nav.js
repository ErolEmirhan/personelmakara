export const MAIN_TABS = {
  TABLES: 'tables',
  ORDERS: 'orders',
  NOTIFICATIONS: 'notifications',
  OTHER: 'other',
};

/** Alt navbar + orta FAB için içerik alt boşluğu */
export const BOTTOM_NAV_PADDING =
  'calc(5.75rem + env(safe-area-inset-bottom, 0px))';

export const BOTTOM_NAV_HEIGHT = '4.25rem';

/** Header altı — tüm ana sekmeler (masalar, siparişler, bildirimler, ayarlar) */
export const MAIN_CONTENT_TOP_PADDING =
  'calc(3.5rem + 10px + env(safe-area-inset-top, 0px))';
